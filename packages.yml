---

- import_playbook: 'homebrew.yml'

- name: Install macOS Packages
  hosts: all

  tasks:
    - homebrew:
        name: "{{ item }}"
        state: latest
      loop:
        - asciinema
        - aws-iam-authenticator
        - docker-compose
        - fzf
        - htop
        - k9s
        - kubectl
        - netcat

    - homebrew_cask:
        name: "{{ item }}"
        state: latest
      loop:
        - docker
        - google-chrome
        - iterm2
        - lens
        - mysqlworkbench
        - wireshark
        - zoom